---
// Hero.astro – Split-screen hero with animated entry
---
<section class="relative min-h-screen grid md:grid-cols-2 pt-[72px] overflow-hidden">

  <!-- Radial glow -->
  <div class="pointer-events-none absolute inset-0
              bg-[radial-gradient(ellipse_60%_80%_at_30%_50%,rgba(201,168,76,0.06)_0%,transparent_70%)]"></div>

  <!-- LEFT: Copy -->
  <div class="relative z-10 flex flex-col justify-center px-16 md:px-20 py-20">

    <!-- Eyebrow -->
    <div class="flex items-center gap-3 mb-7">
      <span class="h-px w-10 bg-gradient-to-r from-luxury-gold to-transparent"></span>
      <span class="text-luxury-gold text-[11px] font-semibold tracking-[3px] uppercase">
        New York's Premier Luxury Practice
      </span>
    </div>

    <!-- H1 -->
    <h1 class="font-serif font-extrabold text-[clamp(48px,5vw,76px)]
               leading-[1.08] tracking-[-1.5px] text-brand-white mb-6">
      Your Smile is<br />
      <span class="bg-gradient-to-br from-luxury-gold-light via-luxury-gold to-luxury-gold-dark
                   bg-clip-text text-transparent">
        Your Most<br />Valuable Asset
      </span>
    </h1>

    <!-- Subheadline -->
    <p class="text-brand-muted text-[17px] font-light leading-[1.75] max-w-[480px] mb-12">
      World-class cosmetic dentistry crafted for those who demand
      <strong class="text-brand-white font-medium">absolute perfection</strong>.
      From porcelain veneers to full-arch implants — every smile is a masterpiece.
    </p>

    <!-- Actions -->
    <div class="flex flex-wrap items-center gap-6 mb-16">
      <a href="#booking"
         class="inline-flex items-center gap-2.5
                bg-gradient-to-br from-luxury-gold to-luxury-gold-dark
                text-deep-slate text-[13px] font-bold tracking-[1.5px] uppercase
                px-9 py-4 rounded-[3px]
                shadow-[0_8px_32px_rgba(201,168,76,0.3)]
                hover:shadow-[0_12px_40px_rgba(201,168,76,0.45)]
                hover:-translate-y-0.5
                transition-all duration-300">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <rect x="3" y="4" width="18" height="18" rx="2"/>
          <line x1="16" y1="2" x2="16" y2="6"/>
          <line x1="8" y1="2" x2="8" y2="6"/>
          <line x1="3" y1="10" x2="21" y2="10"/>
        </svg>
        Schedule Consultation
      </a>
      <a href="#services"
         class="inline-flex items-center gap-2
                border border-luxury-gold/25 text-luxury-gold-light
                text-[13px] font-medium tracking-wide
                px-6 py-4 rounded-[3px]
                hover:border-luxury-gold hover:bg-luxury-gold/[0.06] hover:text-brand-white
                transition-all duration-250">
        Explore Services
        <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <line x1="5" y1="12" x2="19" y2="12"/>
          <polyline points="12 5 19 12 12 19"/>
        </svg>
      </a>
    </div>

    <!-- Stats -->
    <div class="flex gap-10">
      {[
        { num: '2,400+', label: 'Smiles Transformed' },
        { num: '18yr',   label: 'Of Excellence'       },
        { num: '5.0★',  label: 'Patient Rating'       },
      ].map((s, i) => (
        <div class={`relative ${i < 2 ? 'pr-10 border-r border-deep-slate-border/60' : ''}`}>
          <div class="font-serif text-4xl font-bold text-luxury-gold leading-none mb-1">{s.num}</div>
          <div class="text-[11px] text-brand-muted uppercase tracking-[1px]">{s.label}</div>
        </div>
      ))}
    </div>
  </div>

  <!-- RIGHT: Image placeholder -->
  <div class="hidden md:flex items-center justify-center px-10 py-20 pr-20 relative z-10">
    <div class="relative w-full max-w-[520px]">

      <!-- Frame -->
      <div class="w-full aspect-[4/5] rounded-lg border border-deep-slate-border
                  bg-gradient-to-br from-deep-slate-light to-deep-slate-mid
                  flex items-center justify-center overflow-hidden">
        <div class="flex flex-col items-center gap-4 opacity-30">
          <svg class="w-16 h-16 text-brand-muted" fill="none" stroke="currentColor" stroke-width="1" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10"/>
            <path d="M8 14s1.5 2 4 2 4-2 4-2"/>
            <line x1="9" y1="9" x2="9.01" y2="9"/>
            <line x1="15" y1="9" x2="15.01" y2="9"/>
          </svg>
          <p class="text-[11px] tracking-[2px] uppercase text-brand-muted">Smile Transformation Photography</p>
        </div>
      </div>

      <!-- ADA badge -->
      <div class="absolute -bottom-8 -left-6
                  flex items-center gap-3.5 bg-deep-slate-light
                  border border-deep-slate-border rounded-lg px-5 py-4
                  shadow-[0_20px_60px_rgba(0,0,0,0.5)] backdrop-blur-xl">
        <div class="w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0
                    bg-gradient-to-br from-luxury-gold to-luxury-gold-dark">
          <svg class="w-4 h-4 fill-deep-slate" viewBox="0 0 24 24">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
        </div>
        <div>
          <div class="font-serif text-base font-semibold text-brand-white leading-tight">ADA Certified</div>
          <div class="text-[11px] text-brand-muted">American Dental Association</div>
        </div>
      </div>

      <!-- Rating badge -->
      <div class="absolute -top-6 -right-6
                  flex items-center gap-3.5 bg-deep-slate-light
                  border border-deep-slate-border rounded-lg px-5 py-4
                  shadow-[0_20px_60px_rgba(0,0,0,0.5)] backdrop-blur-xl">
        <div class="w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0
                    bg-gradient-to-br from-luxury-gold to-luxury-gold-dark">
          <svg class="w-4 h-4 fill-deep-slate" viewBox="0 0 24 24">
            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
          </svg>
        </div>
        <div>
          <div class="font-serif text-base font-semibold text-brand-white leading-tight">5.0 / 5.0</div>
          <div class="text-[11px] text-brand-muted">847 Verified Reviews</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom gold hairline -->
  <div class="absolute bottom-0 left-0 right-0 h-px
              bg-gradient-to-r from-transparent via-luxury-gold to-transparent opacity-15"></div>
</section>
