---
// Header.astro – Sticky glassmorphism navigation
---
<header
  id="site-header"
  class="fixed top-0 left-0 right-0 z-50 flex items-center justify-between
         px-12 h-[72px] border-b border-luxury-gold/[0.15]
         backdrop-blur-xl bg-deep-slate/70
         transition-all duration-300"
>
  <!-- Logo -->
  <a href="/" class="flex items-center gap-3 no-underline group">
    <div class="w-9 h-9 rounded-full flex items-center justify-center
                bg-gradient-to-br from-luxury-gold to-luxury-gold-dark
                shadow-[0_0_20px_rgba(201,168,76,0.3)]
                font-serif font-bold text-deep-slate text-base">
      E
    </div>
    <span class="font-serif text-lg font-semibold tracking-wide text-brand-white">
      Elite <span class="text-luxury-gold">Dental</span> Studio
    </span>
  </a>

  <!-- Nav links -->
  <nav class="hidden md:flex items-center gap-9">
    {['Services','Our Process','Results','Contact'].map(item => (
      <a href={`#${item.toLowerCase().replace(' ','-')}`}
         class="text-brand-muted hover:text-luxury-gold-light
                text-xs font-medium tracking-[0.8px] uppercase
                transition-colors duration-200">
        {item}
      </a>
    ))}
  </nav>

  <!-- CTA cluster -->
  <div class="flex items-center gap-4">
    <a href="tel:+18004353837"
       class="hidden sm:flex items-center gap-2 text-luxury-gold-light
              hover:text-brand-white text-xs font-medium tracking-wide
              transition-colors duration-200">
      <svg class="w-4 h-4 fill-current" viewBox="0 0 24 24">
        <path d="M6.62 10.79a15.45 15.45 0 0 0 6.59 6.59l2.2-2.2a1 1 0 0 1 1.02-.24
                 11.36 11.36 0 0 0 3.56.57 1 1 0 0 1 1 1V20a1 1 0 0 1-1 1
                 A17 17 0 0 1 3 4a1 1 0 0 1 1-1h3.5a1 1 0 0 1 1 1
                 11.36 11.36 0 0 0 .57 3.56 1 1 0 0 1-.25 1.02z"/>
      </svg>
      (800) ELITE-DS
    </a>
    <a href="#booking"
       class="bg-gradient-to-br from-luxury-gold to-luxury-gold-dark
              text-deep-slate text-[11px] font-bold tracking-[1.2px] uppercase
              px-5 py-2.5 rounded-[3px]
              shadow-[0_4px_20px_rgba(201,168,76,0.25)]
              hover:shadow-[0_6px_30px_rgba(201,168,76,0.45)]
              hover:-translate-y-px
              transition-all duration-250">
      Book Consultation
    </a>
  </div>
</header>

<script>
  const header = document.getElementById('site-header');
  window.addEventListener('scroll', () => {
    header?.classList.toggle('bg-deep-slate/95', window.scrollY > 60);
    header?.classList.toggle('bg-deep-slate/70', window.scrollY <= 60);
  });
</script>
